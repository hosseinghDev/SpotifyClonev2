<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SpotifyClone.Maui.Views.PlaylistPage"
             xmlns:viewmodel="clr-namespace:SpotifyClone.Maui.ViewModels"
             xmlns:model="clr-namespace:SpotifyClone.Maui.Models"
             x:DataType="viewmodel:PlaylistViewModel"
             Title="{Binding Title}" BackgroundColor="#121212">
    <Grid RowDefinitions="Auto, *">
        <Button Grid.Row="0" Text="+ Create Playlist" Command="{Binding CreatePlaylistCommand}" BackgroundColor="#1DB954" TextColor="Black"
                FontAttributes="Bold" Margin="10" HeightRequest="50" CornerRadius="25"/>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Playlists}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Playlist">
                    <Frame Margin="10,5" Padding="15" CornerRadius="10" BackgroundColor="#2a2a2a" BorderColor="Transparent">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:PlaylistViewModel}}, Path=GoToPlaylistDetailCommand}" CommandParameter="{Binding .}"/>
                        </Frame.GestureRecognizers>
                        <Label Text="{Binding Name}" TextColor="White" FontSize="18"/>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <StackLayout Padding="20">
                    <Label Text="You have no playlists. Create one!" TextColor="Gray" HorizontalOptions="Center"/>
                </StackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <ActivityIndicator Grid.Row="1" IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}" Color="#1DB954" VerticalOptions="Center" HorizontalOptions="Center"/>
    </Grid>
</ContentPage>